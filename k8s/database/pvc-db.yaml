apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: pvc-db
  namespace: showcase-platform
spec:
  storageClassName: rook-ceph-block
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

