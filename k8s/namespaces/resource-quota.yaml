apiVersion: v1
kind: ResourceQuota
metadata:
  name: showcase-platform-quota
  namespace: showcase-platform
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 3Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "20"

